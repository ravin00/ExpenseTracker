// TODO: Implement useAuth hook
// This hook provides authentication state and methods

export function useAuth() {
    // TODO: Return auth state and methods (login, logout, isAuthenticated, user)
    return {
        isAuthenticated: false,
        user: null,
        isLoading: false,
    }
}

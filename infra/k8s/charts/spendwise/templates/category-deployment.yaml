apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.categoryService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.categoryService.name }}
    {{- include "spendwise.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.categoryService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.categoryService.name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ .Values.categoryService.name }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Values.categoryService.name }}
        image: {{ include "spendwise.image" (dict "registry" .Values.global.image.registry "repository" .Values.global.image.repository "tag" .Values.categoryService.tag) }}
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        securityContext:
          {{- toYaml .Values.containerSecurityContext | nindent 10 }}
        ports:
        - containerPort: {{ .Values.categoryService.port }}
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: ASPNETCORE_ENVIRONMENT
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "spendwise.secretName" . }}
              key: DB_PASSWORD
        - name: ConnectionStrings__CategoryDb
          value: "Host={{ include "spendwise.dbHost" . }};Port={{ include "spendwise.dbPort" . }};Database={{ .Values.databases.category }};Username={{ include "spendwise.dbUser" . }};Password=$(DB_PASSWORD)"
        - name: Jwt__Secret
          valueFrom:
            secretKeyRef:
              name: {{ include "spendwise.secretName" . }}
              key: JWT_SECRET
        - name: Jwt__Issuer
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: JWT_ISSUER
        - name: Jwt__Audience
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: JWT_AUDIENCE
        resources:
          requests:
            cpu: {{ .Values.categoryService.resources.requests.cpu }}
            memory: {{ .Values.categoryService.resources.requests.memory }}
          limits:
            cpu: {{ .Values.categoryService.resources.limits.cpu }}
            memory: {{ .Values.categoryService.resources.limits.memory }}
        startupProbe:
          httpGet:
            path: /health
            port: {{ .Values.categoryService.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.categoryService.port }}
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.categoryService.port }}
          initialDelaySeconds: 45
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5

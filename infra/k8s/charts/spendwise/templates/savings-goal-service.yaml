apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.savingsGoalService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.savingsGoalService.name }}
    {{- include "spendwise.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.savingsGoalService.name }}
  ports:
  - port: 80
    targetPort: {{ .Values.savingsGoalService.port }}
    protocol: TCP
  type: ClusterIP

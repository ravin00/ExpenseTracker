apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.categoryService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.categoryService.name }}
    {{- include "spendwise.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.categoryService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.categoryService.name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ .Values.categoryService.name }}
    spec:
      containers:
      - name: {{ .Values.categoryService.name }}
        image: {{ include "spendwise.image" (dict "registry" .Values.global.image.registry "repository" .Values.global.image.repository "tag" .Values.categoryService.tag) }}
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.categoryService.port }}
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: ASPNETCORE_ENVIRONMENT
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: spendwise-secrets
              key: DB_PASSWORD
        - name: ConnectionStrings__CategoryDb
          value: "Host={{ include "spendwise.dbHost" . }};Port={{ .Values.postgres.port }};Database={{ .Values.databases.category }};Username={{ include "spendwise.dbUser" . }};Password=$(DB_PASSWORD)"
        - name: Jwt__Secret
          valueFrom:
            secretKeyRef:
              name: spendwise-secrets
              key: JWT_SECRET
        - name: Jwt__Issuer
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: JWT_ISSUER
        - name: Jwt__Audience
          valueFrom:
            configMapKeyRef:
              name: spendwise-config
              key: JWT_AUDIENCE
        resources:
          requests:
            cpu: {{ .Values.categoryService.resources.requests.cpu }}
            memory: {{ .Values.categoryService.resources.requests.memory }}
          limits:
            cpu: {{ .Values.categoryService.resources.limits.cpu }}
            memory: {{ .Values.categoryService.resources.limits.memory }}
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.categoryService.port }}
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.categoryService.port }}
          initialDelaySeconds: 30
          periodSeconds: 15
          failureThreshold: 3

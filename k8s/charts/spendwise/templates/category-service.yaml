apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.categoryService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.categoryService.name }}
    {{- include "spendwise.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.categoryService.name }}
  ports:
  - port: 80
    targetPort: {{ .Values.categoryService.port }}
    protocol: TCP
  type: ClusterIP
